# YepPet

## Fase 1

- Projecte base creat a `Documents/_DATA/repos/yeppet`.
- Estructura superior amb `docs/` i `src/`.
- Frontend Angular 21 generat a `src/Web`.
- Base inicial orientada a `core`, `shared` i `features`.
- Home provisional creada per validar UX i direcció visual.
- Sense backend encara; aquesta fase treballa amb enfocament mock-first.

## Proper pas

- Crear les features `places` i `favorites`.
- Definir models TypeScript i mock services.
- Preparar layout i components reutilitzables.
- Aplicar routing real per a `/places` i `/places/:id`.
